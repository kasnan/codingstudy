#include<iostream>
#include<vector>

using namespace std;

int findfunc(int n);
void unionfunc(int a,int b);

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n,m;
    cin >> n >> m;

    int truthp;
    cin >> truthp;

    for(int i=0;i<truthp;i++){
        int pnum;
        cin >> pnum;
    }
}